{
  "name": "smart-finance-dashboard",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npm run build && python backend/app.py",
    "dev": "cd frontend && npm run dev"
  }
}
